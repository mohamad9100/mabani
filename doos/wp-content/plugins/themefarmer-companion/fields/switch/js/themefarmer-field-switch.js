wp.customize.controlConstructor['themefarmer-switch'] = wp.customize.Control.extend({

    // When we're finished loading continue processing
    ready: function() {

        'use strict';
        // control.setting.set( newValue );
        
    },
});